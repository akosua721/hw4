<!DOCTYPE html>
<html lang="en">

<head>
  <title>HW 4</title>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">

  <!-- Bootstrap Icons -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css" rel="stylesheet">

  <!-- Custom CSS -->
  <link rel="stylesheet" href="/stylesheets/application.css">

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</head>

<body>
  <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container">
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <a class="text-decoration-underline text-primary h7 d-block" aria-current="page" href="/">Home</a>
          </li>
        </ul>
        
        <ul class="navbar-nav mb-2 mb-lg-0">
          <% if user_signed_in? %>
            <li class="nav-item d-inline-block text-primary h7 mx-2 text-black">
              Logged in as: <%= current_user.username %> |
            </li>
            <li class="nav-item">
              <%= link_to 'Logout', destroy_user_session_path, method: :delete, class: "nav-item d-inline-block text-decoration-underline text-primary h7" %>
            </li>
          <% else %>
            <li class="nav-item">
              <%= link_to 'Login', login_path, class: "text-decoration-underline text-primary h7 d-block" %>
            </li>
            <li class="nav-separator">
              <span class="text-primary h7 d-block mx-2 text-black"> | </span>
            </li>
            <li class="nav-item">
              <%= link_to 'Sign Up', signup_path, class: "text-decoration-underline text-primary h7 d-block" %>
            </li>
          <% end %>
        </ul>
      </div>
    </div>
  </nav>

  <div class="container mt-3">
    <% if flash["notice"].present? %>
      <div class="alert alert-primary">
        <%= flash["notice"] %>
      </div>
    <% end %>

    <% if user_signed_in? %>
      <p>Welcome, <%= current_user.username %>!</p>
    <% else %>
      <%= link_to "Login", login_path %>
      <%= link_to "Sign up", signup_path %>
    <% end %>

    <%= yield %>
  </div>
</body>
</html>
